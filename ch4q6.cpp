#include<iostream>
using namespace std;
enum etype{ laborer, secretary, manager, accountant, executive,researcher };
int main()
{
    etype e1;
    char ch;
    cout<<"\n Enter employee type (first letter only) laborer, secretary, manager,accountant, executive, researcher): "; cin>>ch;
    switch(ch);
    {
        case l:
        if(ch=='l')
        cout<<"\n Employee type is "<< e1.1;
    }
    return 0;
}

